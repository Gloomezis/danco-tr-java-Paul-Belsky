'use strict';

socialNetworkApp.controller('DialogMessageController',
    function DialogMessageController($scope,  dialogMessageService, Notification) {


         $scope.getDialogMessages = function () {
            
        };

        $scope.getDialogMessage = function () {
            
        };

        $scope.addDialogMessage = function () {
           
        };

        $scope.editDialogMessage = function () {
           
        };

        $scope.deleteDialogMessage = function () {
           
        };

        
    });