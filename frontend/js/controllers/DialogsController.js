'use strict';

socialNetworkApp.controller('DialogController',
    function DialogController($scope,  dialogService, Notification) {

         $scope.getDialogs = function () {
            
        };

        $scope.getDialog = function () {
            
        };

        $scope.addDialog = function () {
           
        };

        $scope.editDialog = function () {
           
        };

        $scope.deleteDialog = function () {
           
        };

        
    });